# 🚀 메모 관리 시스템 환경 변수 설정
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요
# cp env.example .env

# 서버 포트 (기본값: 8083)
# EC2에서 다른 포트를 사용하려면 이 값을 변경하세요
PORT=8083

# 서버 호스트 (기본값: 자동 감지)
# 0.0.0.0으로 설정하면 모든 네트워크 인터페이스에서 접근 가능
# 특정 IP로 제한하려면 해당 IP 주소를 입력하세요
HOST=0.0.0.0

# Node.js 환경
# development: 개발 환경 (디버그 정보 포함)
# production: 프로덕션 환경 (성능 최적화)
NODE_ENV=production

# API 기본 URL (프론트엔드에서 사용)
# EC2 퍼블릭 IP 주소로 변경하세요
# 예: API_BASE_URL=http://your-ec2-public-ip:8083
# API_BASE_URL=http://your-ec2-public-ip:8083

